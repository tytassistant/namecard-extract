In summary.html

1) do not bold content in the Cards field. 
2) the view button should just be an icon (similar to the delete button). Use exact same icon size as the delete button
3) The new session link should be a primary button (it's own card)
4) Remove "Past name card extraction sessions"
5) The main page title should not change to Session History. It should still be Name Card Extract across all pages. 
6) Add the first card that is showing the history, we need to follow the session history page style in the statement-checker app (e.g. green table, follow table padding and width etc. That statement-checker formating has been very optimized. I want to follow exactly) 
